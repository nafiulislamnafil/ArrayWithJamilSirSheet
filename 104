#include <stdio.h>
int main() {
int t;
scanf("%d",&t);
for(int i = 1;i <=t; i++) {
    int n,cnt=0,odd=0,zero=0;
    scanf("%d", &n);
    int a[n];
    for(int i = 0; i < n; i++) {
          scanf("%d", &a[i]);
          if(a[i]>0){
                cnt++;
            }
            if(a[i]%2!=0){
                odd++;
            }
           if(a[i]==0){
                zero++;
            }
    }
    printf("%d: ",cnt);
    for(int i =0; i<n; i++) {
             if(a[i]>0){
                printf("%d ",a[i]);
            }
    }
    printf("\n");
    printf("%d: ",zero);
     for(int i =0; i<n; i++) {
             if(a[i]==0){
                printf("%d ",a[i]);
            }
    }    
    printf("\n");
    printf("%d: ",odd);
     for(int i =0; i<n; i++) {
             if(a[i]%2!=0){
                printf("%d ",a[i]);
            }
    }
 printf("\n");
 printf("\n");
}
return 0;
}
