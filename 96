#include <stdio.h>
int main() {
int t,l,r,f=0;
scanf("%d", &t);
for(int i = 1; i <=t; i++) {
    int n;
    scanf("%d", &n);
    int a[n];
    for(int i = 0; i < n; i++) {
          scanf("%d", &a[i]);
    }
    scanf("%d%d", &l,&r);
    if(l<=r&&l<n&&r<n){
        for(int i =l; i<=r; i++) {
               printf("%d ",a[i]);
        }
    }
    else{
    printf("-1");
    }
 printf("\n");
}
return 0;
}
